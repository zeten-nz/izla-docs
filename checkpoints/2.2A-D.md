# Phase 2.2A-D — Content Lifecycle / Schema Hardening  (RECON/IMPL COMPLETE — OWNER REVIEW PENDING)

- **Result:** PASS. Two content-schema changes only; full regression green.
- **Code commit SHA (izlan):** `7dec7bff4880fefbbb9698fd222b174a702977bd` (branch `phase/2.2A-D`, off `main` `281ca415`).
- **Docs commit SHA (izla-docs):** branch `phase/2.2A-D` (off `main` `86ebc313`). Governance limitation: a checkpoint
  cannot contain its own final commit SHA (self-referential) — the exact docs commit SHA is reported in the final
  checkpoint response and the PR, not fabricated here.
- **Branch:** `phase/2.2A-D` (both repos). Not merged.
- **Migration count:** 22 (new: `20260821110000_content_schema_hardening`; previous 21).
- **Unit / E2E totals:** 397 / 436 (833). (+4 e2e: the new `content-schema-hardening.e2e-spec.ts`; unit unchanged.)
- **Named CHECK constraints:** 46 (previous 45; +1 `chk_lesson_prerequisite_no_self_loop`).
- **Drift:** clean (empty diff, exit 0 on izlan_dev + izlan_test); `migrate status` up to date on both.

## Schema changes
- **`Lesson.contentKey`** — `String @unique @map("content_key")`; DB `content_key TEXT NOT NULL` + unique index
  `lesson_content_key_key`. Immutable stable business/import identity, DISTINCT from `id`/`slug`/`title`. Migration is
  **staged/deploy-safe**: add nullable → deterministic backfill (`'legacy-' || id::text`, derived from the immutable UUID;
  no title/slug dependence; no invented English/A1 semantics) → `SET NOT NULL` → unique index. (dev/test had 0 rows.)
- **`chk_lesson_prerequisite_no_self_loop`** — `CHECK ("lesson_id" <> "prerequisite_lesson_id")` on `lesson_prerequisite`.
  Self-loop ONLY; corrected the schema comment (multi-node DAG cycle prevention is future service validation, not a DB CHECK).

## Critical invariants covered (test/content-schema-hardening.e2e-spec.ts)
- **CSH-DB-01** — a Lesson persists with a valid unique `contentKey`.
- **CSH-DB-02** — duplicate `contentKey` → DB unique violation (P2002).
- **CSH-DB-03** — `LessonPrerequisite` self-loop (lessonId==prerequisiteLessonId) rejected at the DB CHECK.
- **CSH-DB-04** — a normal non-self edge (B requires A) is allowed.
- Full multi-node DAG cycle is **intentionally not asserted** (deferred to Phase 2.2A service validation).
DB verified (dev+test): `content_key` NOT NULL + unique index present; self-loop CHECK present; 0 NULL, 0 rows (no data recreated).

## Changed paths
- **izlan:** `prisma/schema/content.prisma` (2 edits: `contentKey` + prereq comment), `prisma/migrations/20260821110000_content_schema_hardening/migration.sql` (new), `test/content-schema-hardening.e2e-spec.ts` (new), + `contentKey` added to 13 existing lesson fixtures (daily-mission, daily-plan, daily-plan-review, izl-reward, learner-signals, lesson-completion, lesson-execution, review-candidates, review-mastery, review-session, roadmap, roadmap-progress, xp). (`finance.prisma`/`learning.prisma` `prisma format` churn was reverted — not in the phase.)
- **izla-docs:** `TECH_DECISIONS.md` (TD-240..245), `PROJECT_STATE.md`, `PHASE_HISTORY.md`, `CONTENT_AUTHORING_RECON.md` (§14 status), `checkpoints/2.2A-D.md` (this file).

## TDs added
TD-240 (revision lifecycle), TD-241 (publish authority), TD-242 (published-revision/runtime authority), TD-243 (lifecycle
safety), **TD-244 (content identity/format/concurrency — `contentKey` implemented here)**, **TD-245 (prerequisite invariant
— self-loop CHECK implemented here)**. These formalize the already-accepted CONTENT_AUTHORING_RECON §13a decisions; no new
decision invented; skill merge + media transcript/captions remain deferred (not TD'd).

## Remaining gaps (out of scope — future phases)
- Full-DAG prerequisite cycle validation → Phase 2.2A (service/transaction). `updatedAt` optimistic-concurrency enforcement
  → 2.2A. Activity Registry / shared payload validator → 2.2A-R. Authoring backend / permissions / publish / CMS / bulk
  import → 2.2A/2.2B/2.2C/2.2D. `contentKey` immutability enforcement → future writer (no DB trigger). Telegram cross-surface
  verification gate remains separate. Payment provider track remains PAUSED.

## STOP boundary
Do NOT begin: 2.2A-R, 2.2A authoring backend, 2.2B publishing, 2.2C CMS, 2.2D bulk import, real English lessons, Telegram
implementation/identity/bot/Mini App/Stars, CLICK/Payme, or unrelated auth hardening. Wait for owner review + PR merge.
